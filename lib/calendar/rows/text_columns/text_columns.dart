import 'package:flutter/material.dart';

Widget activityColumn(title) {
  return Column(children: [
    Text(
      title,
      style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
    ),
  ]);
}

Widget dateColumn(title) {
  return Column(children: [
    Text(
      title,
      style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
    ),
  ]);
}

Widget dayColumn(title) {
  return Column(children: [
    Text(
      title,
      style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
    ),
  ]);
}

Widget timeColumn(title) {
  return Column(children: [
    Text(
      title,
      style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
    ),
  ]);
}

Widget otherColumn(title) {
  return Column(children: [
    Text(
      title,
      style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
    ),
  ]);
}
